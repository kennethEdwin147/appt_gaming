# Instructions Tests Auth Unit - Gaming Platform

## 📁 Structure à créer

```
tests/
├── Traits/
│   └── CreatesTestData.php
└── Unit/
    └── Auth/
        ├── AuthControllerTest.php
        ├── CreatorSetupControllerTest.php
        ├── EmailVerificationControllerTest.php
        └── PasswordResetControllerTest.php
```

## 🧪 Créer le trait CreatesTestData

**Fichier :** `tests/Traits/CreatesTestData.php`

**Contenu :** Trait avec méthodes helper :
- `createUser($role = 'customer', $attributes = [])` - Créer utilisateur
- `createCreator($attributes = [])` - Créer créateur avec user
- `createCustomer($attributes = [])` - Créer customer avec user  
- `createEventType($creator = null, $attributes = [])` - Créer event type
- `createAvailability($creator = null, $attributes = [])` - Créer availability
- `createTimeSlot($creator = null, $attributes = [])` - Créer time slot
- `actingAsCreator($attributes = [])` - Login comme créateur
- `actingAsCustomer($attributes = [])` - Login comme customer

## 🔧 Modifier TestCase.php

**Fichier :** `tests/TestCase.php`

**Ajouter :**
- `use RefreshDatabase, CreatesTestData`
- `Mail::fake()` dans setUp()
- `Event::fake()` dans setUp()

## 🎯 Tests AuthControllerTest.php

**Fichier :** `tests/Unit/Auth/AuthControllerTest.php`

**Tests à créer :**

### Tests d'inscription
- `it_can_register_a_client()` - Inscription client réussie
- `it_can_register_a_creator()` - Inscription créateur avec gaming_pseudo
- `it_validates_required_fields_on_registration()` - Validation champs requis
- `it_prevents_duplicate_emails()` - Email unique
- `it_prevents_duplicate_gaming_pseudo()` - Gaming pseudo unique
- `it_sends_verification_email_on_registration()` - Email envoyé

### Tests de connexion
- `it_can_login_with_valid_credentials()` - Login réussi
- `it_rejects_invalid_credentials()` - Login échoué
- `it_redirects_creator_to_setup_after_login()` - Redirect setup si timezone null
- `it_redirects_creator_to_dashboard_when_setup_complete()` - Redirect dashboard si setup OK
- `it_redirects_customer_to_dashboard_after_login()` - Redirect customer
- `it_redirects_to_verification_if_email_not_verified()` - Email non vérifié

### Tests de déconnexion
- `it_can_logout()` - Logout + redirect home
- `it_clears_session_on_logout()` - Session vidée

## 🎯 Tests CreatorSetupControllerTest.php

**Fichier :** `tests/Unit/Auth/CreatorSetupControllerTest.php`

**Tests à créer :**

### Étape 1 - Timezone
- `it_shows_timezone_selection()` - Affichage page timezone
- `it_can_save_timezone()` - Sauvegarde timezone
- `it_redirects_to_profile_after_timezone()` - Redirect étape 2
- `it_skips_timezone_if_already_set()` - Skip si déjà configuré

### Étape 2 - Profile
- `it_shows_profile_setup()` - Affichage page profil
- `it_requires_timezone_before_profile()` - Empêche skip étape 1
- `it_can_save_profile()` - Sauvegarde profil complet
- `it_validates_profile_fields()` - Validation bio, rate, etc.
- `it_marks_setup_as_completed()` - setup_completed_at défini
- `it_redirects_to_dashboard_after_completion()` - Redirect final

### Sécurité
- `it_requires_authentication()` - Middleware auth
- `it_requires_creator_role()` - Seuls les créateurs

## 🎯 Tests EmailVerificationControllerTest.php

**Fichier :** `tests/Unit/Auth/EmailVerificationControllerTest.php`

**Tests à créer :**

### Pages
- `it_shows_verification_notice()` - Page "vérifiez email"
- `it_redirects_verified_users_from_notice()` - Skip si déjà vérifié

### Vérification
- `it_can_verify_email()` - Vérification avec token valide
- `it_rejects_invalid_verification_token()` - Token invalide
- `it_marks_email_as_verified()` - email_verified_at défini
- `it_can_resend_verification_email()` - Renvoi email

### Redirections après vérification
- `it_redirects_creator_to_setup_after_verification()` - Flow créateur incomplet
- `it_redirects_creator_to_dashboard_if_setup_complete()` - Flow créateur complet
- `it_redirects_customer_to_dashboard_after_verification()` - Flow customer

## 🎯 Tests PasswordResetControllerTest.php

**Fichier :** `tests/Unit/Auth/PasswordResetControllerTest.php`

**Tests à créer :**

### Pages
- `it_shows_forgot_password_form()` - Page forgot password
- `it_shows_reset_form_with_valid_token()` - Page reset avec token
- `it_rejects_invalid_reset_token()` - Token invalide

### Envoi link
- `it_can_send_reset_link()` - Envoi link reset
- `it_validates_email_exists()` - Email doit exister
- `it_sends_reset_email()` - Email effectivement envoyé

### Reset password
- `it_can_reset_password()` - Reset réussi
- `it_validates_password_confirmation()` - Confirmation password
- `it_updates_password_hash()` - Hash mis à jour
- `it_redirects_to_login_after_reset()` - Redirect après reset

## 📧 Vérifications emails

**Dans chaque test email :**
```php
// Email envoyé
Mail::assertSent(NotificationClass::class);

// Email envoyé au bon user
Mail::assertSent(NotificationClass::class, function ($mail) use ($user) {
    return $mail->hasTo($user->email);
});

// Contenu email
Mail::assertSent(NotificationClass::class, function ($mail) use ($data) {
    return $mail->data->id === $data->id;
});
```

## 🎮 Données de test gaming

### Créateur gaming
```php
$creatorData = [
    'first_name' => 'ProGamer',
    'last_name' => 'Elite',
    'email' => 'progamer@example.com',
    'password' => 'password123',
    'password_confirmation' => 'password123',
    'gaming_pseudo' => 'ProGamer2024',
];
```

### Setup créateur
```php
$setupData = [
    'timezone' => 'America/Toronto',
    'bio' => 'Coach Valorant expert avec 5 ans d\'expérience',
    'main_game' => 'Valorant',
    'rank_info' => 'Radiant',
    'default_hourly_rate' => 75.00,
];
```

## 📊 Assertions importantes

### Base de données
```php
$this->assertDatabaseHas('users', ['email' => $email]);
$this->assertDatabaseHas('creators', ['gaming_pseudo' => $pseudo]);
$this->assertDatabaseMissing('users', ['email' => $email]);
```

### Authentification
```php
$this->assertAuthenticated();
$this->assertGuest();
$this->assertEquals('creator', auth()->user()->role);
```

### Redirections
```php
$response->assertRedirect('/creator/dashboard');
$response->assertRedirect('/creator/setup/timezone');
$response->assertRedirect('/email/verify');
```

### Validation
```php
$response->assertSessionHasErrors(['email', 'password']);
$response->assertSessionHasNoErrors();
```

### Messages flash
```php
$response->assertSessionHas('success', 'Message attendu');
$response->assertSessionHas('error');
```

## 🚀 Commandes à exécuter

```bash
# Créer les dossiers
mkdir -p tests/Traits tests/Unit/Auth

# Créer les fichiers de test
php artisan make:test Auth/AuthControllerTest --unit
php artisan make:test Auth/CreatorSetupControllerTest --unit
php artisan make:test Auth/EmailVerificationControllerTest --unit
php artisan make:test Auth/PasswordResetControllerTest --unit

# Lancer les tests
php artisan test tests/Unit/Auth
```

## 📝 Structure des tests

**Pattern Arrange-Act-Assert :**
```php
/** @test */
public function it_can_do_something()
{
    // Arrange
    $user = $this->createUser();
    $data = ['key' => 'value'];
    
    // Act
    $response = $this->post('/endpoint', $data);
    
    // Assert
    $this->assertDatabaseHas('table', $data);
    $response->assertRedirect('/success');
}
```

## 🎯 Points critiques à tester

- **Gaming pseudo unique** pour créateurs
- **Flow wizard setup** créateur (timezone → profile)
- **Redirections selon rôle** et état setup
- **Emails Resend** envoyés correctement
- **Sessions gérées** correctement
- **Validation complète** des formulaires
- **Sécurité** (auth, rôles, tokens)

## 📋 Checklist validation

- [ ] Tous les tests passent
- [ ] Coverage > 80% sur Auth controllers
- [ ] Emails mockés correctement
- [ ] Base de données refresh entre tests
- [ ] Pas de side effects entre tests
- [ ] Gaming pseudo testé spécifiquement
- [ ] Setup wizard complet testé
